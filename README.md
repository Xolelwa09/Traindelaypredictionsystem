
  # Train Delay Prediction System

  This is a code bundle for Train Delay Prediction System. The original project is available at https://www.figma.com/design/Ru2dNLU0y6TamcPdWH4sQr/Train-Delay-Prediction-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  